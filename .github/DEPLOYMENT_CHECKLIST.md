# 阿里云部署检查清单

## 📋 部署前检查

### 服务器准备
- [ ] 已购买阿里云 ECS 实例
- [ ] 服务器配置满足最低要求（2核4GB）
- [ ] 已配置安全组规则（开放 80, 443, 22 端口）
- [ ] 已获取服务器公网 IP
- [ ] 可以通过 SSH 连接服务器

### 域名配置（可选）
- [ ] 已购买域名
- [ ] 已配置 DNS 解析指向服务器 IP
- [ ] DNS 解析已生效（可通过 `ping your-domain.com` 验证）

### 代码准备
- [ ] 代码已提交到 Git 仓库
- [ ] 已推送到 GitHub
- [ ] 本地测试通过

---

## 🔧 环境配置检查

### Docker 环境
- [ ] Docker 已安装（`docker --version`）
- [ ] Docker Compose 已安装（`docker-compose --version`）
- [ ] Docker 服务已启动（`systemctl status docker`）

### 配置文件
- [ ] 已创建 `.env` 文件
- [ ] 已修改 MySQL 密码
- [ ] 已生成 NEXTAUTH_SECRET（至少 32 字符）
- [ ] 已配置域名（NEXTAUTH_URL, NEXT_PUBLIC_API_BASE_URL）
- [ ] 已配置 CRON_SECRET
- [ ] 已配置代理服务（如果需要）
- [ ] 已设置 ALLOW_MOCK_SUFFIX=false

### Nginx 配置
- [ ] 已修改 nginx/conf.d/kylink.conf 中的域名
- [ ] SSL 证书已准备（如果使用 HTTPS）

---

## 🚀 部署步骤检查

### 1. 代码部署
- [ ] 已克隆代码到服务器
- [ ] 已切换到正确的分支
- [ ] 已复制环境变量文件

### 2. 构建和启动
- [ ] Docker 镜像构建成功
- [ ] 所有容器启动成功
- [ ] MySQL 容器健康检查通过
- [ ] 应用容器健康检查通过
- [ ] Nginx 容器健康检查通过

### 3. 数据库初始化
- [ ] 数据库迁移执行成功
- [ ] 管理员用户创建成功

### 4. 服务验证
- [ ] 健康检查 API 返回正常（`curl http://localhost:51001/api/health`）
- [ ] Nginx 健康检查通过（`curl http://localhost/health`）
- [ ] 可以访问管理后台
- [ ] 可以登录管理后台

---

## 🔒 安全配置检查

### SSL 证书（如果使用 HTTPS）
- [ ] SSL 证书已获取
- [ ] 证书文件已复制到 nginx/ssl/ 目录
- [ ] Nginx 配置已更新
- [ ] HTTPS 访问正常
- [ ] HTTP 自动重定向到 HTTPS

### 防火墙
- [ ] 阿里云安全组规则已配置
- [ ] 服务器防火墙已配置（如果有）
- [ ] 只开放必要的端口

### 密钥安全
- [ ] 所有密钥都已修改（不使用默认值）
- [ ] .env 文件权限正确（600）
- [ ] 敏感信息未提交到 Git

---

## 📊 监控配置检查

### 日志
- [ ] 可以查看应用日志（`docker-compose logs app`）
- [ ] 可以查看 Nginx 日志（`docker-compose logs nginx`）
- [ ] 可以查看 MySQL 日志（`docker-compose logs mysql`）

### 备份
- [ ] 数据库备份脚本已配置
- [ ] 备份目录已创建
- [ ] 定时备份已设置（可选）

### 监控
- [ ] 服务器资源监控已配置（可选）
- [ ] 应用性能监控已配置（可选）
- [ ] 错误告警已配置（可选）

---

## ✅ 部署后验证

### 功能测试
- [ ] 管理后台可以访问
- [ ] 用户可以登录
- [ ] API 接口正常工作
- [ ] 数据库读写正常
- [ ] 代理服务连接正常（如果配置）

### 性能测试
- [ ] 页面加载速度正常
- [ ] API 响应时间正常
- [ ] 数据库查询性能正常

### 压力测试（可选）
- [ ] 并发请求测试通过
- [ ] 长时间运行稳定

---

## 📝 文档检查

### 部署文档
- [ ] 已阅读完整部署文档
- [ ] 已记录服务器信息
- [ ] 已记录数据库密码
- [ ] 已记录管理员账号

### 运维文档
- [ ] 已了解日常维护命令
- [ ] 已了解故障排查方法
- [ ] 已了解备份恢复流程

---

## 🎯 常用命令

```bash
# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f app

# 重启服务
docker-compose restart app

# 更新应用
git pull && docker-compose build && docker-compose up -d

# 备份数据库
docker exec kylink-mysql mysqldump -u root -p${MYSQL_ROOT_PASSWORD} kyads_suffixpool > backup.sql

# 查看资源使用
docker stats
```

---

## 📞 紧急联系

如果部署过程中遇到问题：

1. 查看日志：`docker-compose logs -f`
2. 检查配置：`docker-compose config`
3. 查看文档：`.github/DEPLOYMENT.md`
4. 提交 Issue：https://github.com/daphnelxqyp/kylink/issues

---

**检查完成后，请保存此清单以备后续参考。**
