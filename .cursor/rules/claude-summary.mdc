---
description: 每次对话后追加一句话到 CLAUDE.md
alwaysApply: true
---
# 对话后总结规则

## 要求
- 每次对话结束前，追加一句话到项目根目录的 `CLAUDE.md`
- 只写一句话，符合 `CLAUDE.md` 现有风格与规则
- 用中文，尽量简洁，描述本次对话完成的关键结果
- 追加到文件末尾，不要改动已有内容

## 例子
2026-01-31：修复 CI 报错并让 `npm run ci` 通过。
